---
import { getCollection } from "astro:content";
import BaseLayout from "../../../layouts/BaseLayout.astro";
import Reveal from "../../../components/Reveal.astro";
import ProjectGrid from "../../../components/ProjectGrid.astro";
import { formatYear } from "../../../lib/formatYear";
import { stripLocale } from "../../../i18n/ui";
import { paletteAttrs } from "../../../lib/palette";

const projects = (await getCollection("product-design"))
  .filter((e) => e.id.startsWith("en/"))
  .sort((a, b) => a.data.order - b.data.order)
  .map((project) => ({
    href: `/en/product-design/${stripLocale(project.id)}`,
    title: project.data.title,
    subtitle: project.data.subtitle,
    cover: project.data.cover,
    coverAnimation: project.data.coverAnimation,
    year: formatYear(project.data, "en"),
    tags: project.data.tags,
  }));
---

<BaseLayout palette="work"
  title="Product Design"
  description="UX design projects by Lital Bar Noy."
>
  <section class="mx-auto max-w-6xl px-6 pt-16 lg:px-10 lg:pt-24" {...paletteAttrs("work")}>
    <Reveal as="h1" class="text-4xl font-bold lg:text-6xl">
      Product Design
    </Reveal>
    <Reveal as="p" class="mt-4 max-w-xl text-lg text-ink-soft">
      Research, prototyping, and usability testing — a selection of projects
      I led end to end.
    </Reveal>
  </section>

  <section class="mx-auto max-w-6xl px-6 py-16 lg:px-10" {...paletteAttrs("work")}>
    <ProjectGrid projects={projects} />
  </section>
</BaseLayout>
