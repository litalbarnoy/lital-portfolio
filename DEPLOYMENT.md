# הוראות פרסום — מהמחשב שלך לאוויר

מסמך זה מסביר איך להעלות את האתר לאינטרנט, שלב אחר שלב. חלק מהשלבים חייבת לבצע את בעצמך (יצירת חשבונות, רכישת דומיין) — קלוד קוד לא ממלא סיסמאות או פרטי תשלום בשבילך.

---

## שלב 1: GitHub — גיבוי וניהול גרסאות

1. היכנסי ל-[github.com](https://github.com) ולחצי "Sign up" (חינם). תצטרכי מייל, שם משתמש וסיסמה.
2. אחרי ההרשמה, צרי repository חדש: לחצי על "+" למעלה → "New repository" → תני לו שם כמו `lital-portfolio` → השאירי אותו **Private** או **Public** (זה עניין שלך) → **אל תסמני** "Add a README" (כבר יש לנו קבצים) → "Create repository".
3. בעמוד שנפתח יופיע קישור repository בפורמט `https://github.com/USERNAME/lital-portfolio.git` — שלחי לי אותו, ואני אחבר את הפרויקט המקומי ואעלה (push) את הקוד — **רק אחרי אישור שלך בצ'אט**.

הפרויקט כבר מוכן מבחינה טכנית: יש לו `git init` וקומיט ראשון עם כל הקבצים (חוץ מ-`node_modules` ו-`dist` שלא צריך).

---

## שלב 2: Vercel — אחסון האתר

1. גשי ל-[vercel.com](https://vercel.com) ולחצי "Sign Up" → **"Continue with GitHub"** (מתחברת עם אותו חשבון GitHub, לא צריך סיסמה נפרדת).
2. בדשבורד: "Add New" → "Project" → בחרי את ה-repository `lital-portfolio`.
3. Vercel מזהה אוטומטית שזה פרויקט Astro. אין צורך לשנות שום הגדרה — פשוט לוחצים **"Deploy"**.
4. אחרי כ-דקה תקבלי כתובת חינמית כמו `lital-portfolio.vercel.app` — האתר באוויר!
5. **מרגע זה, כל פעם שתשמרי שינוי דרך מערכת הניהול (או שאני אעלה שינוי בשמך) — Vercel יפרסם אותו אוטומטית תוך דקה, בלי שום פעולה נוספת.**

> ⚠️ הערה על התוכנית החינמית של Vercel ("Hobby"): היא מיועדת רשמית לשימוש אישי/לא-מסחרי. אתרי פורטפוליו אישיים משתמשים בה בלי בעיה בפועל, אבל אם בעתיד האתר יגדל להיות חלק מעסק פעיל עם הרבה תנועה, שווה לשקול שדרוג לתוכנית "Pro" (משתלמת, כמה דולרים בחודש).

---

## שלב 3: דומיין — הכתובת שלך

קנית את הדומיין **דרך Wix עצמו**, כלומר הוא רשום אצלם. **אין צורך להעביר בעלות** (domain transfer) — הדרך הפשוטה היא להשאיר את הדומיין רשום ב-Wix, ופשוט לשנות לאן הוא "מצביע" (DNS), כך שבמקום להראות את אתר ה-Wix הוא יראה את האתר שבנינו ב-Vercel.

### 3.1 קודם: הוסיפי את הדומיין בצד של Vercel

1. בפרויקט ב-Vercel: **Settings → Domains** → הקלידי את הדומיין שלך (למשל `litalbarnoy.com`) → **Add**.
2. Vercel יציג לך רשומת DNS להזנה (בדרך כלל `A` record שמצביע ל-`76.76.21.21`, ולפעמים גם `CNAME` בשביל `www`). **תעתיקי בדיוק את הערכים שהוא מראה** — הם ספציפיים לפרויקט שלך.

### 3.2 אחר כך: עדכני את ה-DNS בממשק של Wix

1. היכנסי לחשבון ה-Wix שלך → לכי לעמוד **"Domains"** (זה נפרד מעורך האתר עצמו — יש תפריט ניהול דומיינים כללי בחשבון).
2. ליד הדומיין הרלוונטי, לחצי על **סמל שלוש הנקודות ("Domain Actions")** → בחרי **"Manage DNS records"**.
3. גללי לרשומות מסוג **A** ו-**CNAME**:
   - לעדכון/הוספת רשומת **A**: לחצי **"+ Add Record"**, השאירי את שדה **Host Name ריק** (אל תכתבי "@" גם אם מוצע), הזיני את הכתובת שנתן לך Vercel, לחצי **Save** ואז **Save Changes**.
   - לרשומת **CNAME** (בד"כ עבור `www`): אותו תהליך, עם הערך ש-Vercel נתן.
4. אם כבר קיימות רשומות A/CNAME ישנות של Wix על אותו שם — צריך **לערוך** אותן (לא רק להוסיף חדשות), אחרת יהיה קונפליקט. לחיצה על **"Edit"** ליד הרשומה הקיימת.

**זמן התעדכנות:** בדרך כלל דקות עד מספר שעות, לפעמים עד 48 שעות.

לאחר שה-DNS מתעדכן, Vercel יזהה זאת אוטומטית וינפיק אישור אבטחה (HTTPS) בעצמו — בלי פעולה נוספת מצידך.

> את אתר ה-Wix הישן אפשר להשאיר כרגע (הוא פשוט יפסיק לקבל תנועה ברגע שהדומיין יצביע ל-Vercel), ולבטל את המנוי שם בנפרד כשתהיי מוכנה.

---

## שלב 4: מערכת הניהול (Decap CMS) — גרסה חיה

כרגע מערכת הניהול (`/admin`) עובדת **רק מקומית** במחשב שלך (דרך `npm run cms`). כדי לערוך תוכן **מהאתר החי** (מכל מחשב, בלי להריץ כלום מקומית), צריך לחבר אימות דרך GitHub — זה שלב טכני נפרד שנעשה **אחרי** שהאתר באוויר. תזכירי לי כשתהיי מוכנה ואסביר/אבנה את זה.

---

## סיכום — מה נדרש ממך עכשיו (בסדר הזה)

1. ליצור חשבון GitHub ולשלוח לי את קישור ה-repository (שלב 1 למעלה).
2. ליצור חשבון Vercel (עם GitHub) ולחבר את הפרויקט (שלב 2).
3. להוסיף את הדומיין ב-Vercel ולעדכן את ה-DNS בממשק ה-Domains של Wix (שלב 3).

אני אעשה את כל השאר: ה-push בפועל לגיטהאב (רק אחרי אישור שלך בצ'אט), ומעקב שהכל עלה תקין.
