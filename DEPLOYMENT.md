# הוראות פרסום — מהמחשב שלך לאוויר

מסמך זה מסביר איך להעלות את האתר לאינטרנט, שלב אחר שלב. חלק מהשלבים חייבת לבצע את בעצמך (יצירת חשבונות, רכישת דומיין) — קלוד קוד לא ממלא סיסמאות או פרטי תשלום בשבילך.

---

## שלב 1: GitHub — גיבוי וניהול גרסאות

1. אם אין לך חשבון: היכנסי ל-[github.com](https://github.com) ולחצי "Sign up" (חינם).
2. צרי repository חדש: לחצי על "+" למעלה → "New repository" → תני לו שם כמו `lital-portfolio` → השאירי אותו **Private** או **Public** (זה עניין שלך) → **אל תסמני** "Add a README" (כבר יש לנו קבצים) → "Create repository".
3. בעמוד שנפתח יופיע קישור repository בפורמט `https://github.com/USERNAME/lital-portfolio.git` — שלחי לי אותו, ואני אחבר את הפרויקט המקומי ואעלה (push) את הקוד — **רק אחרי אישור שלך בצ'אט**.

הפרויקט כבר מוכן מבחינה טכנית: יש לו `git init` וקומיט ראשון עם כל הקבצים (חוץ מ-`node_modules` ו-`dist` שלא צריך).

---

## שלב 2: Vercel — אחסון האתר

1. גשי ל-[vercel.com](https://vercel.com) ולחצי "Sign Up" → **"Continue with GitHub"** (מתחברת עם אותו חשבון GitHub, לא צריך סיסמה נפרדת).
2. בדשבורד: "Add New" → "Project" → בחרי את ה-repository `lital-portfolio`.
3. Vercel מזהה אוטומטית שזה פרויקט Astro. אין צורך לשנות שום הגדרה — פשוט לוחצים **"Deploy"**.
4. אחרי כ-דקה תקבלי כתובת חינמית כמו `lital-portfolio.vercel.app` — האתר באוויר!
5. **מרגע זה, כל פעם שתשמרי שינוי דרך מערכת הניהול (או שאני אעלה שינוי בשמך) — Vercel יפרסם אותו אוטומטית תוך דקה, בלי שום פעולה נוספת.**

> ⚠️ הערה על התוכנית החינמית של Vercel ("Hobby"): היא מיועדת רשמית לשימוש אישי/לא-מסחרי. אתרי פורטפוליו אישיים משתמשים בה בלי בעיה בפועל, אבל אם בעתיד האתר יגדל להיות חלק מעסק פעיל עם הרבה תנועה, שווה לשקול שדרוג לתוכנית "Pro" (משתלמת, כמה דולרים בחודש).

---

## שלב 3: דומיין — הכתובת שלך (litalbarnoy.com או דומה)

### קודם: את כבר בעלים של דומיין?

ציינת שיש לך אתר קיים ב-Wix. **חשוב לברר קודם**: איפה **רשום** הדומיין שלך (לא איפה האתר מתארח, אלא איפה **קנית** את הכתובת עצמה)?

- אם קנית את הדומיין **דרך Wix עצמו** — הוא רשום אצלם, וצריך "לשחרר" אותו (domain transfer) או פשוט לעדכן את הגדרות ה-DNS שלו כדי שיצביע ל-Vercel (לא חובה להעביר בעלות, מספיק לשנות לאן הוא מצביע).
- אם קנית אותו **דרך ספק חיצוני** (GoDaddy, Namecheap וכו') וחיברת אותו ל-Wix — אז הניהול כבר אצל הספק החיצוני, וזה קל יותר.

**תגידי לי איפה הדומיין רשום ואכין לך הוראות מדויקות לאותו ממשק בדיוק (צילומי מסך של השלבים הרלוונטיים).**

### אם עדיין אין לך דומיין (רכישה חדשה)

**הדרך הכי פשוטה למי שלא מכיר בניית אתרים: לקנות ישירות דרך Vercel** ([vercel.com/domains](https://vercel.com/domains)):
- מחפשים את השם הרצוי (למשל `litalbarnoy.com`), קונים (כ-10-15$ לשנה עבור .com).
- Vercel מגדיר הכל **אוטומטית** — אין צורך לגעת ב-DNS בכלל. זה היתרון הגדול: אפס הגדרות טכניות.

**חלופה (בדרך כלל מעט יותר זולה, אך דורשת 2-3 צעדים ידניים):** לקנות ב-[Namecheap](https://namecheap.com), ואז ב-Vercel → Project → Settings → Domains → להוסיף את הדומיין. Vercel יראה לך רשומת DNS (בד"כ `A` שמצביעה ל-`76.76.21.21`) להזין בממשק של Namecheap תחת "Advanced DNS". התעדכנות: דקות עד כמה שעות.

בשני המקרים, Vercel מנפיק אישור אבטחה (HTTPS) אוטומטית וחינם.

---

## שלב 4: מערכת הניהול (Decap CMS) — גרסה חיה

כרגע מערכת הניהול (`/admin`) עובדת **רק מקומית** במחשב שלך (דרך `npm run cms`). כדי לערוך תוכן **מהאתר החי** (מכל מחשב, בלי להריץ כלום מקומית), צריך לחבר אימות דרך GitHub — זה שלב טכני נפרד שנעשה **אחרי** שהאתר באוויר. תזכירי לי כשתהיי מוכנה ואסביר/אבנה את זה.

---

## סיכום — מה נדרש ממך עכשיו

1. ליצור חשבון GitHub (אם אין) ולשלוח לי את קישור ה-repository.
2. ליצור חשבון Vercel (עם GitHub) ולחבר את הפרויקט.
3. לענות לי איפה הדומיין הנוכחי שלך רשום (Wix עצמו / ספק אחר) — או להגיד אם את רוצה לקנות דומיין חדש.

אני אעשה את כל השאר: הכנת הקוד, הוראות מדויקות לכל שלב, וה-push בפועל (רק אחרי אישור שלך).
